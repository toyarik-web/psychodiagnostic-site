# Serve index.html by default and prevent directory listing
DirectoryIndex index.html index.htm index.php
Options -Indexes

# Sensible MIME types (some hosts misconfigure)
AddType text/css .css
AddType application/javascript .js
AddType image/svg+xml .svg

# If the site is installed in the domain root, leave rewrites off
# Uncomment to force HTTPS and www (optional)
# RewriteEngine On
# RewriteCond %{HTTPS} !=on [OR]
# RewriteCond %{HTTP_HOST} !^www\.psychodiagnostic\.online$ [NC]
# RewriteRule ^ https://www.psychodiagnostic.online%{REQUEST_URI} [L,R=301]

# Cache-busting and no-transform
<IfModule mod_headers.c>
  <FilesMatch "\.(js|css)$">
    Header set Cache-Control "no-cache, no-store, must-revalidate, max-age=0"
    Header set Pragma "no-cache"
    Header set Expires "0"
  </FilesMatch>
</IfModule>
